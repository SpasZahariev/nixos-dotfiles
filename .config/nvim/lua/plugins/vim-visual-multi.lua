return {
    -- multi cursor support for vim
    'mg979/vim-visual-multi',
    init = function()
        -- vim.g.VM_maps = {
        --     ['Find Under'] = '<C-n>'
        -- }
        -- vim.g.VM_add_cursor_at_pos_no_mappings = 1
    end,
}
