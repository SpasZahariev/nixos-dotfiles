return {
	-- A little ui plugin to show a nice litte textfield instead of on the command line. E.G when I'm creating files
	"stevearc/dressing.nvim",
	event = "VeryLazy", -- this plugin is not important to startup -> it can load veeery late into the startup
}
